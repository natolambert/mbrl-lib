defaults:
  - algorithm: pets
  - agent: sac
  - dynamics_model: ensemble
  - overrides: pets_ball_rolling # pets_ipv2 # pets_cartpole

seed: 0
device: "cuda:0"
log_frequency_model: 1
log_frequency_agent: 1000
save_video: false
debug_mode: false

experiment: default

hydra:
  run:
    dir: ./exp/${algorithm.name}/${experiment}/${overrides.env}/${now:%Y.%m.%d}/${now:%H%M}
